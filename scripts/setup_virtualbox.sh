#!/bin/bash

set -euo pipefail

sudo apt-get install -y virtualbox virtualbox-ext-pack
# At first stage - press enter multiple times and then yes.
# Then again but instead of yes, create a new password.

